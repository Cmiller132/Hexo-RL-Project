# Overnight Phase 2/3 Autotune Monitor - 2026-04-28

## 17:35 EDT Heartbeat Health Check

- Heartbeat ran without requesting approval/escalation.
- Stable supervisor PID `6660`, watchdog PID `6829`, local dashboard, and LAN proxy are all alive.
- Current stage is `3B_static_asha`; dashboard shows `asha_02_best_graph_option1` active with graph architecture and live throughput around `25.6 positions/sec`.
- Resource status is healthy for this host: WSL has about `11 GiB` RAM available, swap remains low at about `196 MiB`, GPU is active around `51%` with about `4.9 GiB / 12.3 GiB` VRAM used.
- The post-restart ASHA loop is no longer stuck after pruned trials. Latest completed graph trials have clean candidate recall (`1.0`), zero truncation, and no illegal/crash rate.
- Best current score after restart is `asha_01_best_graph_option1` at about `0.495`; `asha_00_best_graph_option1` is close at about `0.464` and has self-play around `1659 positions/min`.
- ResTNet was again retained through calibration but quarantined from long ASHA by the loosened throughput gate: about `207.8 positions/min` versus graph at about `648.0 positions/min`, below the `0.35` gate. This is fair and expected under the current safety/performance policy.
- No new timeout, Rust panic, OOM, candidate-recall, or non-finite-loss issue appeared after the restart. No action taken.

## 13:45 EDT Heartbeat Prompt Fix And Stuck ASHA Intervention

- Updated the heartbeat automation so it must never request escalation/user approval. Future checks should use non-escalated shell/dashboard probes only; if WSL is blocked by a sandbox boundary, the check should fall back to dashboard/Windows status instead of prompting.
- The stable run progressed into `3B_static_asha` and produced useful comparisons before stalling. Best trial remained graph: `asha_00_best_graph_option1` scored about `0.506`, with epoch-2 self-play around `2014 positions/min`, no truncation, no illegal/crash rate, and candidate recall metrics at `1.0`.
- `asha_02_best_graph_option1` also looked healthy, scoring about `0.485`, with epoch-2 self-play around `1450 positions/min`, candidate recall at `1.0`, and zero truncation.
- A CNN control trial, `asha_03_best_current_33`, was correctly weak/slow in its startup sweep: `2` workers at only about `130 positions/min`, then hit a non-finite value loss during batch 12 and was pruned/released.
- Root cause of the multi-hour freeze was an ASHA control-flow bug: after `_train_trial_epoch()` pruned a trial, the loop condition `while trial.epoch < resource` stayed true forever because a pruned trial's epoch no longer advances.
- Patched `scripts/run_phase3_48h_autotune.py` so ASHA, PBT, and champion loops stop training/evaluating a trial after it is pruned. Verified with `python -m py_compile` on Windows and `python3 -m py_compile` in WSL.
- Killed the wedged supervisor/watchdog pair and restarted on the same stable run root. New supervisor PID is `6660`; new shell watchdog PID is `6829`.
- The restarted run is back in `3A_calibration` using cached runtime sweeps, so it should move through calibration faster. Dashboard remains live locally and over LAN.
- Current optimization judgement: graph is still the best use of this machine. The CNN/ResTNet controls are valuable as comparisons, but CNN long-search/full-aux at 192 moves is both far slower and less numerically stable than graph.

## 13:10 EDT Stability Intervention And Stable Restart

- Previous mixed-family run root `/root/hexo_runs/phase2_phase3_autotune_mixed_compare_20260428_ext4` hit full WSL memory and swap saturation during `asha_03_best_restnet_33`, a non-graph ResTNet ASHA trial with `full_sims=1200`.
- Symptoms were WSL at roughly `23 GiB / 23 GiB` RAM plus `12 GiB / 12 GiB` swap, followed by Rust self-play leaf expansion warnings: `Inference server did not respond within 30000ms` around moves `130-132`.
- I restarted WSL with `wsl --shutdown` to clear wedged workers and CUDA memory, then patched `scripts/run_phase3_48h_autotune.py` with a host guard for this machine: non-graph low-memory CUDA trials above `800` full simulations are pruned, and non-graph runtime startup sweeps cap workers at `2`.
- New active stable run root is `/root/hexo_runs/phase2_phase3_autotune_mixed_compare_stable_20260428_ext4`.
- Stable restart command preserved the important comparison choices: `max_game_moves=192`, calibration throughput gate `0.35`, runtime sweep workers `2,3,4,5`, ASHA resources `2,5,10`, and champion minimum epochs `20`.
- The stable run is alive: supervisor PID `568`, shell watchdog PID `782`, dashboard PID `785`.
- Dashboard is live at `http://127.0.0.1:8765`; LAN proxy remains live at `http://192.168.68.62:8766`.
- First stable startup sweep is working as intended. For `cal_best_current_33`, it only tested safe 2-worker non-graph candidates and selected `workers=2`, `max_batch_size=80`, `max_wait_us=500`, at about `476 positions/min`.
- Memory after restart is healthy: roughly `20 GiB` WSL RAM available and zero swap used. GPU is active during self-play at about `3.9 GiB` VRAM with moderate utilization.
- Current judgement: this was not a model-quality failure; it was a runtime safety failure from allowing too-large non-graph long-search trials on a 12 GB GPU plus 24 GB WSL memory. The new guard keeps CNN/ResTNet in the comparison at `800` sims while preventing the exact runaway that wedged WSL.

## 12:24 EDT Mixed-Family Update

- Active WSL ext4 run root is now `/root/hexo_runs/phase2_phase3_autotune_mixed_compare_20260428_ext4`; dashboard is live at `http://127.0.0.1:8765` and LAN proxy is live at `http://192.168.68.62:8766`.
- App heartbeat automation was recreated as `overnight-phase-2-3-autotune-monitor`, scheduled every 30 minutes, thread-attached, with `gpt-5.5` requested and explicit no-prompt autonomous intervention instructions.
- The monitoring job is orchestration, not only liveness: each check should judge throughput, GPU/CPU feeding, runtime sweep choices, fairness of CNN/ResTNet/graph comparisons, pruning behavior, candidate recall, non-finite metrics, dashboard health, and whether a small safe config/code change is warranted.
- WSL supervisor is alive as PID `8942`; shell watchdog is alive as PID `10050`.
- Current stage is `3B_static_asha`. Calibration completed and ASHA now includes graph, ResTNet, and CNN trials under the loosened `0.35` calibration throughput gate.
- Calibration summary on the mixed run: CNN selected 5 workers at about `584 positions/min`; ResTNet selected 5 workers at about `461 positions/min`; graph selected 5 workers at about `1182 positions/min`.
- Graph calibration remains the best scored calibration result so far: `cal_best_graph_option1`, scheduler score about `0.4073`, no illegal/crash rate, candidate recall gate passed, outside-window missing mass near zero.
- `asha_00_best_graph_option1` completed epoch 1 in `243.97s`, with self-play at about `1363 positions/min`, training at about `1.86 batches/s`, `loss_total ~32.90`, dense policy top-1 `~0.099`, sparse policy top-1 `~0.324`, and candidate recall metrics all `1.0`.
- Current dashboard status around 12:24 EDT: `3B_static_asha`, current trial `asha_00_best_graph_option1`, current model `best_graph_option1`, total saved suite progress `44` games / `8068` positions, dashboard rate about `25 positions/s`.
- Warning/error scan was clean: no timeout, panic, OOM, non-finite, killed worker, or traceback matches in the supervisor log.
- GPU snapshots can look low between self-play chunks because the supervisor is repeatedly starting/stopping short sweep/epoch workers; judge utilization over completed epoch timings and inference-server summaries, not a single idle-second `nvidia-smi` sample.

## Earlier Overnight Context

- Previous WSL ext4 run root: `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4`
- Windows symlink: `runs/phase2_phase3_autotune_overnight_20260428_ext4_active`
- Stable search shape as of ~10:25 EDT: graph-only long search with 3 workers, PBT population auto-capped to 3 on low-RAM WSL, and non-calibration `max_game_moves <= 80`.
- WSL2 RAM was raised via `C:\Users\epicm\.wslconfig` from the default ~15 GiB to `24GB` memory plus `12GB` swap; verified inside WSL as ~23.46 GiB total.
- Fresh restarted run began at ~10:26 EDT. Dense calibration is active and progressing with GPU around `72%`, ~3.2 GiB GPU memory used, and ~14 GiB WSL RAM still available after the first few minutes.
- At ~10:33 EDT, dense calibration completed in `190.8s`, ResTNet calibration completed in `162.5s`, graph calibration was running, no warnings were present, and swap remained unused.
- At ~10:39 EDT, graph calibration completed in `85.7s` for 1024 positions. Dense and ResTNet were quarantined by graph-relative throughput gates and their runtime state was released. ASHA started graph-only with six graph trials.
- At ~10:50 EDT, graph ASHA trials were completing under budget: `asha_00` at `358.0s` / ~`1355 positions/min`, `asha_01` at `324.5s` / ~`995 positions/min`. No warnings and WSL swap still unused.
- At ~11:05 EDT, the suite-aware dashboard was launched at `http://127.0.0.1:8765` against `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4`. It aggregated `19` trial databases, `2678+` saved games, and ranked best checkpoints from the live run root.
- At ~11:18 EDT, the run was restarted with the full pair-policy implementation synced into the WSL ext4 repo and `--max-game-moves 192`. The first post-fix epoch completed as `6` games / `1152` positions, confirming the active cap is now `192` rather than the old `80`/`128` clamp.
- Learning-curve policy update: 1-4 epoch scores are treated as stability/throughput screens only, not proof of strategic strength. ASHA should now use `2,5,10` epoch rungs, promote broadly, and promising/champion candidates should reach `20+` epochs before final trust.
- The earlier 3-worker / 96-move guard got through ASHA, but PBT later wedged when it reused calibration-era trials and reached move `82+` / `114+` under full WSL memory pressure.
- Current intervention: restart from a clean WSL state with PBT restricted to eligible/unquarantined families and pruned/quarantined trial runtime released immediately.
- Stable capped graph throughput before the PBT stall was roughly `950-1870 self-play positions/min` across graph variants; PBT graph `256 token / 1 layer` hit `~1873 positions/min` before the next population member stalled.

## Interventions

2026-04-28 14:33 EDT automation check blocked before WSL inspection. Attempted WSL-only probes for `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4`, including file metadata, supervisor/monitor PIDs, GPU/RAM/swap, and the dashboard API curl from inside WSL; each `wsl -e bash -lc ...` command failed immediately with `Wsl/Service/CreateInstance/E_ACCESSDENIED`. Supervisor command flags (`--max-game-moves 192`, `--asha-resources 2,5,10`, `--perturb-interval 5`, `--champion-min-epochs 20`), logs, events, monitor markdown, dashboard health, and resource status could not be verified from this Codex session. No restart or code/config intervention was attempted because WSL process creation is denied at the service boundary.

2026-04-28 12:32 EDT automation check blocked before WSL inspection. Both `wsl -e bash -lc ...` and `wsl --status` failed with `Wsl/Service/CreateInstance/E_ACCESSDENIED` / `Wsl/EnumerateDistros/Service/E_ACCESSDENIED`, so supervisor/monitor PIDs, logs, events, GPU, WSL RAM/swap, dashboard status, and command-line flags could not be verified from this Codex session. No restart or code/config intervention was attempted because WSL access is required and denied at the service boundary.

2026-04-28 13:33 EDT automation check again blocked before WSL inspection. `wsl -d Ubuntu -- bash -lc ...`, `wsl --list --verbose`, and `wsl --status` all failed with `Wsl/.../Service/E_ACCESSDENIED`, so the run root `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4`, supervisor/monitor PIDs, launch flags, logs, events, GPU status, WSL RAM/swap, and dashboard API could not be verified from this Codex session. No restart, monitor relaunch, or code/config change was attempted because WSL access is denied at the service boundary.

1. Moved the active run to the WSL ext4 repo copy to avoid `/mnt/d` DrvFS instability.
2. Disabled Codex app heartbeat automation and used terminal/session monitoring instead.
3. Added a calibration throughput gate so slow latency-sensitive families are excluded from long ASHA/PBT.
4. Added a 12 GB / 16-core host guard blocking combined ResTNet+sparse policy, after it repeatedly stalled with 30s inference timeouts.
5. Added a 12 GB graph-capable host guard blocking non-graph sparse controls, after sparse CNN also stalled in long ASHA despite passing short calibration.
6. Removed graph sparse-prior stage 1 from the overnight ASHA ladder on this host after stage 1 measured much slower than stage 0.
7. Added dense long-search worker caps, then escalated to graph-relative quarantine for dense CNN because dense long ASHA still timed out even at 4 workers.
8. Added hard pruning for ASHA/PBT epochs exceeding `1.20x` the target epoch budget.
9. Tightened the graph-relative calibration gate to quarantine dense CNN from long search when graph is much faster; dense calibration remains recorded as a baseline.
10. Removed graph sparse-prior stage 1 from the overnight ASHA ladder on this 12 GB / 16-core host after repeated measurements showed it was safe but too slow.
11. Added a non-calibration long-trial cap of `max_game_moves <= 96` on 12 GB CUDA hosts after PBT hit repeated inference timeouts around moves `117-118`.
12. Added a global 12 GB CUDA worker cap of 3 workers / small batches after WSL reported only ~15 GiB RAM and the kernel OOM-killed Python workers.
13. Added host memory detection to the runtime profile so WSL RAM pressure is visible in manifests and reports.
14. Added a low-RAM WSL guard that auto-caps PBT population from `8` to `3` on this 12 GB CUDA / ~15 GiB WSL host.
15. Fixed PBT seeding so it only uses currently eligible, unquarantined families; previously it carried dense CNN and ResTNet calibration trials into long PBT even after graph-relative quarantine.
16. Fixed per-epoch move caps so calibration-created configs are re-capped when reused in ASHA/PBT/champion stages; this prevents 128-move calibration configs from leaking into long PBT.
17. Added runtime release for pruned/quarantined trials so old trainers and replay buffers do not accumulate across ASHA rungs and PBT.
18. Tightened the low-RAM non-calibration game cap to `80` moves after the latest stall showed repeated timeouts at move `82` and WSL memory+swap saturation.
19. Added `C:\Users\epicm\.wslconfig` with `memory=24GB`, `swap=12GB`, and `processors=32`, then restarted WSL and verified the new memory limit.
20. Fixed dashboard visibility for autotune suites: games and checkpoints were already being saved per trial in `trials/<trial>/dashboard.sqlite3`, but the dashboard only read one default DB. Added suite aggregation endpoints, a Suite tab, best-model ranking, recent saved games, and run-aware replay loading.
21. Synced the full Phase 2 pair-policy path into the WSL ext4 repo before PBT could sample `graph_tactical`: pair targets, pair replay tensors, `PairPolicyHead`, pair loss, trainer metrics, and tactical candidate recall gates are now present in the active codebase.
22. Raised the autotune game length to `192` and removed the low-memory override that forced non-calibration runs to `96`/`80`. Also changed the config application path so `--max-game-moves` is the actual requested length instead of `min(base_config, requested)`.
23. Changed the tuning schedule to avoid overvaluing early learning: ASHA resources are `2,5,10`, ASHA promotion keeps the top half, PBT mutates after `5` epochs, and champion selection now prefers candidates with at least `20` epochs.
24. Added a per-config startup runtime sweep before a trial's first real epoch. It runs short self-play probes, tests worker counts from `2,3,4,5` by default, scores by positions/min with GPU snapshots, caches the best tuple by model/static recipe, and applies the selected `num_workers`, `batch_size_per_worker`, `max_batch_size`, and `max_wait_us` automatically.
25. Loosened the calibration throughput quarantine from graph-relative `0.85x` to configurable `0.35x` by default, so current CNN and ResTNet stay in the comparison unless they are more than roughly `3x` slower or fail a hard safety gate.
26. Expanded the dashboard suite view with current model/activity, positions/sec, readable timestamps, model/config inspection, architecture summaries, runtime sweep details, and checkpoint config metadata.
27. Added a low-memory CUDA non-graph full-simulation guard after the 1200-sim ResTNet ASHA trial saturated WSL RAM and swap. On this host, non-graph ASHA/PBT trials now stay at `800` full simulations and non-graph startup sweeps cap workers at `2`; graph configs can still sweep broader worker counts.
28. Re-pointed the thread heartbeat automation at the stable run root, kept the `gpt-5.5` request, and kept the checks thread-attached so future 30-minute wakeups preserve this orchestration context.
29. Fixed ASHA/PBT/champion loops so pruned trials do not keep retraining forever. This specifically addressed the freeze after `asha_03_best_current_33` hit a non-finite value loss.

## Performance Findings

- Graph stage 0 is the only family that is both fast and stable on this machine overnight.
- Dense CNN short calibration is stable at roughly `545-565 positions/min`, but long ASHA with high simulation counts repeatedly times out in late moves.
- ResTNet short calibration is stable but slower, roughly `350-400 positions/min`, and is consistently below the graph-relative throughput gate.
- After WSL RAM expansion, fresh calibration measured dense at ~`322 positions/min`, ResTNet at ~`378 positions/min`, and graph at ~`717 positions/min`; graph remained the clear long-run candidate.
- Sparse CNN short calibration can pass candidate recall, but long ASHA stalls with inference timeouts and poor GPU utilization.
- Graph sparse-prior stage 1 can be made safe enough to measure at `256 tokens / 1 layer`, but it is CPU-bound and scored poorly versus graph stage 0.
- Best graph stage-0 ASHA trials are currently in the `1000-1800 positions/min` range depending on token budget and simulation count.
- Graph-only PBT is currently the first configuration that has run for an extended period with no timeout warnings.
- Late-game positions are the remaining expensive edge: timeouts repeatedly appeared after move ~110, then again at move ~82 under full memory pressure, so overnight optimization now avoids spending most wall time there.
- The limiting resource is not just GPU: WSL memory pressure can kill Python workers. The run now prioritizes stable 3-worker graph throughput over unstable 4-worker peaks.
- The best practical overnight profile on this WSL setup has been graph stage-0 with guarded self-play workers and small inference batches. With WSL RAM now expanded and `max_game_moves=192`, the current preference is to guard simulation count and worker count before shortening games again.
- Worker count is no longer only heuristic. New trials should emit `runtime_sweep_start`, `runtime_sweep_result`, and `runtime_sweep_selected` events, plus `runtime_sweep_cache.json`, so different model sizes can select different CPU/GPU feeding settings.

## Next Watch Items

- Confirm the restarted run logs `pbt_population=3` and no blocked-family PBT population members.
- Watch for non-finite losses after long graph training epochs.
- Watch whether non-finite loss remains isolated to CNN/full-aux controls or appears in graph trials. If graph also shows this, inspect value targets/loss scaling immediately.
- Watch GPU utilization and memory during graph `512 token / 2 layer` configs; these are stable so far but slower than `256 token / 1 layer`.
- Watch for any self-play warnings in 192-move games, especially after move `120`. If warnings return, prefer narrowing workers/batch/wait or simulation count before lowering the game length again.
- Watch for the first `graph_tactical` trial to confirm `loss_pair_policy`, `pair_policy_top1_acc`, and decisive candidate recall metrics are emitted.
- Treat sub-10-epoch results as screening signals only. Use 10 epochs for early architecture comparison, then require 20+ epochs on promising models before trusting strategy/strength conclusions.
- If the mixed-family stable run remains healthy for an hour at 192 moves, let the 48h run continue without further narrowing.
- Confirm runtime sweeps choose a stable worker count for graph 256/384/512 token configs; if `5` workers wins but warnings return in late-game positions, narrow the worker list or increase the probe length before reducing max game length.
- The next clean run should be treated as a mixed-family comparison again: graph may still win, but CNN and ResTNet should no longer be dropped just for a moderate calibration speed gap.

2026-04-28 15:34 EDT automation check remained blocked before WSL inspection. `wsl -e bash -lc ...` failed with `Wsl/Service/CreateInstance/E_ACCESSDENIED`, so the run root `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4`, supervisor/monitor PIDs, required launch flags, logs, events, GPU status, WSL RAM/swap, and dashboard API could not be verified from this Codex session. No restart, monitor relaunch, or code/config change was attempted because WSL access is denied at the service boundary.

2026-04-28 16:36 EDT automation check remained blocked before WSL inspection. Retried the requested WSL-only probe for `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4`, covering supervisor/monitor PID files, required supervisor launch flags, `supervisor.log`, `events.jsonl`, `overnight_monitor.md`, `nvidia-smi`, WSL RAM/swap, and dashboard API curl from inside WSL. The command failed immediately with `Wsl/Service/CreateInstance/E_ACCESSDENIED`, before any Linux-side check could run. No supervisor restart, monitor relaunch, code change, or config change was attempted because WSL instance creation is denied from this Codex session.

2026-04-28 18:38 EDT automation check still blocked before WSL inspection. Attempted the requested WSL-only run probe against `/root/hexo_runs/phase2_phase3_autotune_overnight_20260428_ext4` from `/root/Hexo-RL-Project-ext4`, then separately checked `wsl --status` and `wsl -l -v`. All failed before Linux startup with `Wsl/Service/E_ACCESSDENIED` or `Wsl/EnumerateDistros/Service/E_ACCESSDENIED`. Supervisor and monitor PID status, required launch flags (`--max-game-moves 192`, `--asha-resources 2,5,10`, `--perturb-interval 5`, `--champion-min-epochs 20`), logs/events/monitor markdown, GPU status, WSL RAM/swap, and dashboard API health could not be verified. No restart, monitor relaunch, code change, or config change was attempted because WSL service access is denied from this session.

2026-04-28 20:12 EDT dashboard/LAN check confirmed the stable mixed-family run has advanced from `3B_static_asha` into `3C_pbt`. ASHA narrowed the population to three live trials after 10-epoch graph comparisons; graph `256 token / 1 layer` remains the clear active path. Current PBT self-play is live on `cal_best_graph_option1` with `5/5` workers, cached runtime sweep `workers=5`, `max_batch_size=104`, `max_wait_us=500`, and recent measured throughput around `1889 self-play positions/min` / `31.5 positions/sec`. Candidate recall, outside-window missing mass, truncation rate, and non-finite-loss checks are clean. Windows `nvidia-smi` showed RTX 4070 Ti active at ~`54%` GPU utilization, ~`4.3 GB` VRAM, and ~`135 W`. Direct WSL probes remain blocked by `Wsl/Service/E_ACCESSDENIED`, so this heartbeat used dashboard/LAN plus Windows GPU telemetry only.

2026-04-28 20:47 EDT memory intervention. The active WSL instance reached `23Gi/23Gi` RAM used and `12Gi/12Gi` swap used. Process inspection showed the autotune tree had multiple large Python runtimes alive at once (`~15.7GiB`, `~9.2GiB`, and `~7.0GiB` RSS-equivalent processes before cleanup), and the supervisor log showed PBT had seeded `cal_best_current_33` from calibration into the long PBT population. That dense CNN control was then running 192-move self-play with only `2` workers and repeated `Inference server did not respond within 30000ms` warnings around late moves, while graph trial runtimes/replay buffers were still resident. Root cause: PBT seeding accepted calibration-only checkpoint trials, so slow calibration controls could re-enter long PBT even though ASHA did not promote them. Fix: patched `scripts/run_phase3_48h_autotune.py` so PBT restricts its seed population to ASHA survivors when any exist, and only falls back to calibration/recommended seeds if ASHA produced no survivors. Synced the patch into `/root/Hexo-RL-Project-ext4`, compiled it, killed the saturated/accidental duplicate supervisors and orphaned spawned children, and relaunched the stable run root. Post-cleanup memory was back to ~`3.5GiB` used with ~`19GiB` available and only ~`240MiB` swap used. Note: the active supervisor restarted from the beginning of the schedule; previous checkpoints/events remain on disk for analysis, but the live process is now clean and patched.

2026-04-28 21:05 EDT fairness bug fix. The scheduler was still unfair to `best_current_33` and `best_restnet_33`: calibration throughput gates and a `1.20x` epoch-time hard prune treated "slower than graph" as an exclusion criterion, and static ASHA candidate sampling could be dominated by graph recipes. Patched the tuner so slow calibration now logs `family_throughput_below_gate` without quarantining, ASHA candidate generation is family-balanced, ASHA promotions keep the best valid trial per family via a family floor, and slow-but-valid epochs are scored rather than hard-pruned unless they exceed `3.0x` target time with poor/no score. Also changed low-memory non-graph recipes from fixed `800/1200` simulations to `128/256/384` so CNN/ResTNet can train real 192-move games without late-game inference timeouts. Synced/compiled in WSL and restarted the stable run root. The new calibration is using `best_current_33` at `256` full sims / `96` PCR sims; the first runtime probe completed `384` positions in ~`30.8s` (`~756 positions/min`) with memory stable around `2.8GiB` used and ~`20GiB` available.

2026-04-28 22:04 EDT dashboard/GPU heartbeat. Direct WSL process/log/memory inspection from this Codex heartbeat hit `Wsl/Service/E_ACCESSDENIED`, so no Linux-side restart or file sync was attempted. Dashboard and LAN proxy were responsive for `/root/hexo_runs/phase2_phase3_autotune_mixed_compare_stable_20260428_ext4`. The live run is in `3B_static_asha` on `asha_02_best_graph_option1` using the graph architecture with runtime sweep-selected `4` workers, `max_batch_size=96`, and `max_wait_us=500`. Self-play was live at `94.7%` of the current epoch, `32` games, `5985` positions, `4/4` workers, and about `21.44 positions/sec`. Windows GPU telemetry showed RTX 4070 Ti at ~`41%` GPU utilization, ~`5.1GB/12GB` VRAM, ~`126W`, and `56C`. No intervention was needed from the accessible signals.

2026-04-28 22:18 EDT high-sim fairness correction. Confirmed the prior stable run was still unfair to dense CNN/ResTNet: the host guard treated this WSL setup as low-memory because it reports ~`23.46GB` RAM and a 12GB GPU, then forced non-graph trials down to `128/256/384` sims and capped startup sweep to `<=2` workers with `8` leaf states per worker. That made CNN/ResTNet look underpowered and left CUDA mostly underfed. Patched `scripts/run_phase3_48h_autotune.py` so dense CNN/ResTNet candidates use `800/1200` sims again, recommended non-graph recipes start at `800` sims / `192` PCR sims, high-sim non-graph startup sweeps test `2-6` workers, and high-sim non-graph batches use `16` leaf states per worker with batch caps up to `192`. Synced/compiled in WSL and replaced the old stable run with `/root/hexo_runs/phase2_phase3_autotune_mixed_compare_highsim_fair_20260428_ext4`. The first corrected CNN `800`-sim runtime probe is healthy: `6` workers completed `1152` positions in ~`115.5s` (`~599 positions/min`) with no timeout, and the live GPU sample during the following probe reached ~`96%` utilization / ~`4.6GB` VRAM. This supports the user's point: high-sim dense CNN is possible here; the blocker was orchestration policy, not model feasibility.

2026-04-28 22:24 EDT value-target/game-length correction. Updated the Phase 3 supervisor and launcher so 384 moves is the default game limit and the full-search sim ladder is `512/800/1200/1600`, with PCR/low-search sims `128/192/256/384`. This removes the old 192-move ceiling that could starve the value head of terminal outcomes and restores a higher-quality MCTS floor. Runtime probes now use `768` states by default so a 384-move game limit still samples more than one long game. Restarted into `/root/hexo_runs/phase2_phase3_autotune_384moves_simsweep_20260428_ext4` with `MAX_ACTIVE_TRIALS=12`, `MAX_GAME_MOVES=384`, `RUNTIME_SWEEP_STATES=768`, and `RUNTIME_SWEEP_WORKERS=2,3,4,5,6`. Manifest confirms `max_game_moves=384`, `full_sims=[512,800,1200,1600]`, and `pcr_low_sims=[128,192,256,384]`. First live corrected sweep is CNN `800/192` with six workers; GPU telemetry during startup showed ~`96%` utilization, ~`4.9GB/12GB` VRAM, and ~`182W`.

2026-04-28 22:41 EDT dashboard recovery. The suite dashboard was down because the WSL process tree had died and the PID files for supervisor/dashboard/monitor were stale. A first dashboard-only restart exposed a PowerShell-to-WSL quoting bug that wrote `/dashboard.log` and passed malformed CLI arguments; recovery now uses a bash script piped into WSL with literal run-root paths. Restarted the dashboard on `0.0.0.0:8765`, restarted the 384-move/high-sim supervisor for `/root/hexo_runs/phase2_phase3_autotune_384moves_simsweep_20260428_ext4`, and restarted `scripts/monitor_phase_autotune.sh`. Verified both `http://127.0.0.1:8765/api/suite/status` and `http://192.168.68.62:8766/api/suite/status` respond. Live state is `3A_calibration`, trial `cal_best_current_33`, model `best_current_33`; supervisor, dashboard, and monitor PIDs are alive. A follow-up sample showed GPU utilization around `86%`, ~`3.9GB/12GB` VRAM, and six Rust workers running. The dashboard is back; counters remain at zero only because the restarted 384-move/800-sim calibration has not completed its first game yet.

2026-04-28 23:58 EDT dashboard crash root-cause fix. The repeated dashboard "crashes" were not Flask/front-end exceptions; WSL was being destabilized by memory pressure from the high-sim runtime sweep. Evidence: before the restart, `service_keepalive.log` showed RAM at `23Gi/23Gi` used, `12Gi/12Gi` swap used, then stale dashboard/supervisor PIDs after a WSL reset. A 6-worker dense CNN 384-move/800-sim probe later reproduced the cliff with memory telemetry: `max_used_gb=20.74`, `min_available_gb=2.72`, and `unsafe=true`, while also scoring worse than 3-4 workers. Fixes applied and synced to `/root/Hexo-RL-Project-ext4`: runtime sweep cache now rejects old high-search entries without memory telemetry; each sweep candidate records `/proc/meminfo` samples; memory-unsafe candidates are excluded from selection; after a memory-unsafe candidate, same-or-larger worker counts are skipped; low-memory WSL high-search non-graph sweeps are capped at 5 workers, including fallback wait-time candidates; the keepalive wrapper stops the supervisor before RAM/swap saturation can reset WSL; dashboard polling now avoids overlapping requests and refreshes less aggressively. Verified the new candidate list is `2,3,4,5,5(wait=800)` with no 6-worker entry, dashboard health is green, and WSL memory is stable with swap at `0B`. Current CNN 800-sim probes show 4 workers is the best safe setting so far at roughly `744 positions/min`; 5 workers is slower in the latest probe. This supports high-sim CNN/ResTNet feasibility, but the automatic tuner must avoid the 6-worker RAM cliff on this WSL configuration.
