# Phase 00 S4 Deletion And Legacy Inventory

Date: 2026-04-29

Scope: S4 Data/Train/Eval baseline inventory for replay, buffer, training, evaluation,
dashboard, tuning, config-bearing inputs, and stale runtime deletion ownership.

Owned V2 row: `V2-004`.

Owner mapping source:

| Owner phase | Matrix rows and phase-doc basis |
|---|---|
| Phase 01 | `V2-011` through `V2-016`; `PHASE_01.md` owns engine boundary, legal/history/D6 contracts, source/hash/schema rules, and removal of production fallbacks. |
| Phase 02 | `V2-020` through `V2-025`; `PHASE_02.md` owns candidate, pair, and graph semantic/projection convergence. |
| Phase 03 | `V2-030` through `V2-035`; `PHASE_03.md` owns model registry, `TrainAdapter`, checkpoint manager, model factory deletion, and trainer branch deletion. |
| Phase 04 | `V2-040` through `V2-046`; `PHASE_04.md` owns inference protocol, request-kind dispatch, transport lifecycle, and submit-path deletion. |
| Phase 05 | `V2-050` through `V2-057`; `PHASE_05.md` owns `PolicyProvider`, `PairStrategy`, and `EngineAdapter` MCTS call ownership. |
| Phase 06 | `V2-060` through `V2-065`; `PHASE_06.md` owns `GameRunner` and `SelfPlayWorker` cleanup. |
| Phase 07 | `V2-070` through `V2-075`; `PHASE_07.md` owns replay cutover and old `buffer` runtime deletion for self-play, sampler, training, and epoch paths. |
| Phase 08 | `V2-080` through `V2-086`; `PHASE_08.md` owns evaluation, dashboard inspectors, and typed autotune/recipe cutover. |
| Phase 09 | `V2-090` through `V2-100`; `PHASE_09.md` owns final CI/import enforcement after phase-owned deletions. |

No deletion-owner ambiguity was found during S4 inspection. No runtime code or tests were edited.

Required category coverage below: Python legal fallbacks; compact history parsers; D6 helpers; duplicate protocol parsers; candidate duplication; pair mini-contracts; graph semantic/tensor colocation; checkpoint cleanup; inference architecture dispatch; trainer class branches; dashboard reconstruction; autotune raw config mutation; old replay/buffer runtime paths.

## Legacy Runtime Paths

| Category | Path | Symbol or pattern | Current behavior | Risk | Owner phase | Expected deletion or replacement | Blocking tests or evidence before deletion |
|---|---|---|---|---|---|---|---|
| Python legal fallback | `Python/src/hexorl/buffer/sampler.py` | `HAS_ENGINE`, `_py_encode_from_coords`, `_encode_position_fallback`, `_fallback_legal_moves`, `_py_decode_compact_record`, `_encode_tensor_meta` fallback branch | If `_engine` is unavailable, sampler rebuilds tensors and legal rows in Python from compact history. | Training can consume legal rows that did not come from the Rust boundary; row identity/order/source cannot be validated. | Phase 01 for fallback removal; Phase 07 for sampler/runtime replay cutover | Replace with Rust-backed `engine/legal.py` and `contracts/legal.py`; sampler reads new replay and projects through `replay/projector.py`; Python fallback allowed only in fixtures/tools. | Rust legal parity, malformed history negative tests, source enforcement tests, sampler import audit proving no production fallback, `V2-012`, `V2-071`, `V2-072`, `V2-073`. |
| Python legal fallback | `Python/src/hexorl/dashboard/replay.py` | `_fallback_legal_moves`, `_game_from_moves`, `_engine_encoding`, `_engine_threat_moves`, `get_replay_position` | Dashboard reconstructs positions and falls back to Python legal move generation when the engine path is unavailable. | Dashboard can display/debug a state that diverges from runtime contracts and hide engine boundary failures. | Phase 08, with Phase 01 legal contract dependency | Replace with read-only `ContractInspector` services over `MoveHistory` and `LegalActionTable`; no dashboard-private legal generation. | Dashboard parity tests, inspector route tests, banned private reconstruction import audit, source/hash/version display assertions, `V2-081`, `V2-082`. |
| Python legal fallback | `Python/src/hexorl/graph/batch.py` | `_engine_state_from_history`, `legal_moves_for_stones` fallback in `build_graph_batch_from_history` | Global graph builder uses Rust when importable, otherwise computes legal moves from Python stones. | Graph tokens, legal rows, and training targets may be built from a different legal authority than search/replay. | Phase 01 for Rust/contract source; Phase 02 for graph builder split | `GraphSemanticBuilder` consumes validated legal contract only; fallback legal generation deleted or fixture-only. | Graph/legal parity, graph schema tests, banned legal rebuild audit, corruption tests for stale legal hashes, `V2-012`, `V2-023`, `V2-024`, `V2-025`. |
| Python legal fallback | `Python/src/hexorl/action_contract/tactical_oracle.py` | `legal_moves_from_stones`, `scan_tactical_oracle_from_history(... allow_python_fallback=True)` | Tactical oracle can compute legal/tactical facts without engine replay when explicitly allowed. | Runtime callers may normalize a degraded source instead of failing hard on missing Rust or malformed history. | Phase 01 | Keep tactical facts behind engine/contracts boundary; any Python-only mode becomes fixture/test-only with explicit source labels. | Tactical oracle source-enforcement tests, fixture opt-in tests, runtime import audit, `V2-012`, `V2-016`. |
| Compact history parser | `Python/src/hexorl/action_contract/tactical_oracle.py` | `parse_history_state`, `engine_game_from_history`, repeated `int.from_bytes(..., "little", signed=True)` | Parses compact `(player, q, r)` triples and validates duplicate cells/player order locally. | Duplicates `crates/hexgame-py/src/protocol.rs` history row semantics and can diverge from Rust decode rules. | Phase 01 | Replace runtime parsing with `MoveHistory` plus `engine/history.py`; keep only fixture/test helpers outside runtime. | Encode/decode parity, malformed-byte tests, invalid history tests, duplicate/current-player negative tests, direct parser audit, `V2-013`. |
| Compact history parser | `Python/src/hexorl/graph/batch.py` | `parse_history`, `transform_history`, `struct.unpack_from("<iii", ...)` | Graph builder parses and transforms compact history itself. | Graph can be validated against its own parser instead of the canonical history contract. | Phase 01 and Phase 02 | `MoveHistory` owns parse; `contracts/symmetry.py` owns D6; graph builder consumes contract projections. | History contract tests, D6 parity, graph projection tests, no private compact-history parser audit, `V2-013`, `V2-014`, `V2-023`. |
| Compact history parser | `Python/src/hexorl/dashboard/replay.py` | `decode_move_history`, `encode_move_history`, `_moves_from_bytes` | Dashboard decodes/encodes histories and rebuilds replay positions. | Dashboard debug can disagree with replay/projector contracts and mask malformed bytes. | Phase 08, dependent on Phase 01/07 contracts | Dashboard reads `MoveHistory`/replay inspector output; raw encode/decode only in fixtures/offline import. | Dashboard replay parity, malformed history display failure tests, banned reconstruction audit, `V2-081`, `V2-085`. |
| Compact history parser | `Python/src/hexorl/selfplay/rgsc.py` | `decode_move_history`, `encode_move_history`, `restore_game_from_history` | RGSC owns restart-history decode/replay validation. | Restart path can become a second history authority during self-play. | Phase 01 for history contract; Phase 06 for RGSC lifecycle cleanup | RGSC consumes `MoveHistory`/`EngineAdapter` replay services and remains lifecycle/search-control only. | RGSC lifecycle tests, no private history parser audit in self-play, stale history negative tests, `V2-013`, `V2-060`, `V2-064`. |
| Compact history parser | `Python/src/hexorl/buffer/sampler.py` and `Python/src/hexorl/buffer/targets.py` | `_history_stones`, `_last_move_qr`, `_history_len`, `_dominant_axis_label`, `_legal_qr_from_history`, `parse_history` import | Replay sampling and target assignment inspect compact histories directly. | Training target construction can silently diverge from canonical history/legal/pair semantics. | Phase 07, with Phase 01 dependency | New replay records preserve history identity; `replay/projector.py` reconstructs contracts through engine/history APIs. | Trace-to-record-to-projector identity tests, sample-to-loss smoke, old buffer import audit, `V2-071`, `V2-074`. |
| D6 helper | `Python/src/hexorl/buffer/sampler.py` | `_hex_transform`, `_transform_history_bytes`, `_py_apply_d6_symmetry`, `_transform_dense_policy`, `_transform_policy_v2`, `_transform_pair_policy_v2`, `_transform_axis_label`, `_transform_axis_maps` | Replay sampler applies D6 to histories, dense tensors, sparse targets, pair targets, and axis maps. | D6 semantics are duplicated and tied to sampler-private tensor layout; target mass/known-first semantics can drift. | Phase 01 for canonical D6; Phase 07 for replay projection use | Move D6 to `contracts/symmetry.py`; sampler/projector uses contract transforms only. | Rust/Python D6 parity, inverse/composition tests, target mass preservation, sampler D6 mutation tests, `V2-014`, `V2-074`. |
| D6 helper | `Python/src/hexorl/graph/batch.py` | `transform_qr`, `transform_history`, `transform_policy_target`, `transform_pair_policy_target` | Graph module owns its own coordinate/history/pair transform helpers. | Graph D6 may not match training/replay/dashboard D6 and graph builder owns semantics plus transform. | Phase 01 and Phase 02 | `contracts/symmetry.py` owns D6; graph tensorizer receives transformed contracts only. | Graph D6 inverse/composition/corruption tests, no private D6 audit, `V2-014`, `V2-025`. |
| D6 helper | `Python/src/hexorl/dashboard/app.py` | `_d6_contract_payload`, `_d6_target_checks`, imports `_transform_history_bytes` from sampler | Dashboard produces D6 debug payloads by invoking sampler/private graph helpers. | Dashboard D6 views can validate private transforms instead of canonical contracts. | Phase 08, with Phase 01 dependency | Dashboard D6 view delegates to `ContractInspector` over `contracts/symmetry.py`. | Dashboard D6 view parity tests, display hash/source/version, banned sampler-private import audit, `V2-081`, `V2-085`. |
| Duplicate protocol parser | `Python/src/hexorl/buffer/ring.py` | `_pack_policy_blob`, `_unpack_policy_blob`, `_pack_v2_blob`, `_unpack_v2_blob`, `_pack_legal_blob`, `_unpack_legal_blob`, `_pack_pair_blob`, `_unpack_pair_blob`, `np.frombuffer` | Ring buffer owns ad hoc blob encodings for policies, legal rows, and pair rows. | Old replay rows can be decoded without schema/version/source/hash policy and bypass Rust FFI protocol identity. | Phase 07 | Replace with `replay/codec.py` and versioned replay records; old codec only in migration or frozen fixtures. | Replay codec round-trip/corruption tests, old decode absent from runtime import audit, migration quarantine notes, `V2-070`, `V2-072`, `V2-073`. |
| Duplicate protocol parser | `Python/src/hexorl/buffer/sampler.py` | `np.frombuffer(legal_bytes, dtype=np.int32).reshape(-1, 2)`, manual `to_bytes` legal rows | Sampler decodes Rust legal bytes and can synthesize legal bytes when using fallback. | Python owns row-width interpretation outside the PyO3 protocol owner. | Phase 01 and Phase 07 | Decode through engine/contracts protocol wrappers; projector validates legal row hashes before training. | Malformed FFI byte tests, legal row identity tests, projector corruption tests, import audit, `V2-012`, `V2-071`. |
| Duplicate protocol parser | `Python/src/hexorl/selfplay/worker.py` | `_legal_bytes_from_qr`, repeated `np.frombuffer(legal_bytes, dtype=np.int32).reshape(-1, 2)`, manual `move_history.extend(...to_bytes...)` | Worker decodes/encodes legal rows and move history bytes while wiring MCTS, graph, pair, and replay. | Self-play can mutate or remap Rust rows before search/replay without a contract identity boundary. | Phase 05 for MCTS/policy mapping; Phase 06 for worker cleanup; Phase 01 for protocol wrappers | `GameRunner`/`EngineAdapter` consumes validated legal/history contracts; worker is lifecycle/IPC only. | EngineAdapter row-identity tests, self-play worker import/code audit, behavior debug bundle, `V2-051`, `V2-060`, `V2-064`. |
| Duplicate protocol parser | `Python/src/hexorl/dashboard/model_cache.py` and `Python/src/hexorl/dashboard/replay.py` | `np.frombuffer(legal_bytes, dtype=np.int32).reshape(-1, 2)`, `_moves_from_bytes` | Dashboard/model cache decodes engine legal bytes and move-history bytes for model input/debug. | Dashboard model input may not match training adapter/projector input. | Phase 08 | Dashboard model input/output views use inspector services backed by contract/projector/adapters. | Dashboard/training input parity tests, no private reconstruction import audit, `V2-081`, `V2-085`. |
| Candidate duplication | `Python/src/hexorl/action_contract/candidates.py` | `CandidateBatch`, `build_candidate_set`, `build_candidate_batch` | Current candidate authority lives under `action_contract`, with candidate feature and target mass logic. | Name/location conflicts with V2 `contracts/candidates.py`; multiple consumers call it directly and can bypass future validation/hash policy. | Phase 02 | Replace with `CandidateContractBuilder` and `CandidateTable` in `contracts/candidates.py`; remove `action_contract` runtime imports. | Cross-consumer golden equality, extension-proof tests, import/deletion audit for `action_contract`, `V2-020`, `V2-024`. |
| Candidate duplication | `Python/src/hexorl/buffer/sampler.py` | ReplayDataset root/leaf candidate build around `build_candidate_batch` plus sampler-local tactical/critical extraction | Sampler constructs candidate tensors and diagnostics while sampling old replay records. | Training sees sampler-private candidate semantics instead of canonical replay projector output. | Phase 02 for builder; Phase 07 for replay projector | Candidate construction centralized; sampler reads new records and `replay/projector.py` projects candidates. | Sample-to-loss tests, projection equality, candidate corruption tests, buffer import audit, `V2-020`, `V2-071`. |
| Candidate duplication | `Python/src/hexorl/selfplay/worker.py` | `_critical_actions_from_root_tensor`, `build_candidate_batch` calls in root/leaf paths, `_candidate_forward_rows` | Worker builds candidate rows and sparse priors directly. | Search and replay may record candidate rows that do not match training/dashboard candidates. | Phase 02 for candidate authority; Phase 05/06 for worker/search ownership | `PolicyProvider`/`GameRunner` consumes shared candidates; worker no longer builds candidates. | Self-play/replay/training/dashboard equality tests, worker no-candidate-builder audit, `V2-024`, `V2-060`. |
| Candidate duplication | `Python/src/hexorl/dashboard/app.py`, `Python/src/hexorl/dashboard/model_cache.py`, `Python/src/hexorl/dashboard/replay.py` | `_graph_debug_payload`, `_candidate_rows_debug`, direct `build_candidate_batch` | Dashboard reconstructs candidate rows for debug/model-cache views. | Dashboard can present different candidates than replay/projector/trainer used. | Phase 08, with Phase 02 dependency | Dashboard `ContractInspector` delegates to read-only candidate inspector over canonical contracts. | Dashboard fixture parity, inspector extension proof, banned private reconstruction audit, `V2-081`, `V2-085`. |
| Pair mini-contracts | `Python/src/hexorl/action_contract/candidates.py` | `PairCandidateBatch`, `_canonical_pair`, `build_pair_candidate_batch` | Pair rows are candidate-index pairs, not a canonical phase-aware `PairActionTable`. | First-placement unordered and second-placement known-first semantics can be conflated. | Phase 02 | Delete or demote as projection-only from `PairActionTable`; no independent semantics. | Pair phase tests, D6 pair tests, import/deletion audit for `PairCandidateBatch`, `V2-021`, `V2-022`. |
| Pair mini-contracts | `Python/src/hexorl/graph/batch.py` | `GraphTokenType.PAIR_ACTION`, `graph_batch_with_reference_pair_rows`, `_target_for_pairs`, `_pair_first_target_for_legal`, `PAIR_CHUNK_LIMIT` | Graph builder creates pair tokens/reference pair rows and pair targets inside graph semantics. | Parallel global pair rows can diverge from crop pair rows and future `PairActionTable`. | Phase 02 | Pair rows derive only from `PairActionTable`; graph semantic builder links to pair contract; tensorizer projects. | Crop/global pair equality tests, projection-only tests, pair corruption tests, `V2-021`, `V2-023`, `V2-025`. |
| Pair mini-contracts | `Python/src/hexorl/selfplay/worker.py` | `_score_graph_pair_chunks`, `_score_crop_pair_chunks`, `_pair_qr_from_graph_batch`, `_pair_logits_to_action_logits`, `pair_prior_mix` use | Worker selects, chunks, scores, and applies pair priors directly. | Pair scoring can become a model/config side effect and create full-pair performance traps. | Phase 05 for `PairStrategy`; Phase 06 for worker cleanup | All pair scoring goes through explicit `PairStrategy`; worker no longer owns pair chunks. | No-implicit-pair tests, pair cap tests, worker pair-chunk audit, MCTS telemetry sample, `V2-052`, `V2-053`, `V2-060`. |
| Graph semantic/tensor colocation | `Python/src/hexorl/graph/batch.py` | `GraphBatch`, `build_graph_batch_from_history`, `_build_relations`, `collate_graph_batches`, `validate_graph_ipc_capacity` | One module parses history, builds legal/tactical/candidate/pair graph semantics, creates tensors, validates IPC capacity, and collates batches. | No clean boundary for semantic identity vs tensor projection; dashboard/training/inference can import private graph internals. | Phase 02 | Split into `graph/semantic_builder.py`, `graph/tensorize.py`, and `graph/collate.py`; contracts own graph semantics and hashes. | Graph projection tests, graph schema tests, banned private rebuild audit, extension-proof token/relation tests, `V2-023`, `V2-024`. |
| Checkpoint cleanup and load compatibility | `Python/src/hexorl/model/network.py` | `strip_compiled_prefix`, `load_model_state(... allow_partial=False/True)` | Runtime strips `_orig_mod.` prefixes and supports partial/non-strict loads. | Checkpoint incompatibility can be hidden; model family/protocol/action contract identity is not manifest-enforced. | Phase 03 | `models/checkpoint.py::CheckpointManager` owns strict save/load/inspect; old conversion only offline. | Manifest round-trip, inspect-without-weights, strict load rejection tests, prefix-cleanup audit, `V2-034`. |
| Checkpoint cleanup and load compatibility | `Python/src/hexorl/inference/server.py` | `_state_to_cpu` strips `_orig_mod.`, `load_model_state` in startup/update path | Inference accepts state dicts and cleans prefixes before model load. | Inference server can accept checkpoint/state dicts that do not declare protocol/request-kind contracts. | Phase 03 for checkpoint manager; Phase 04 for inference manifest | Inference requires checkpoint manifest and negotiated `InferenceProtocolManifest`; no prefix cleanup in runtime. | Checkpoint/inference protocol mismatch tests, architecture-string audit, prefix-cleanup audit, `V2-034`, `V2-040`. |
| Checkpoint cleanup and load compatibility | `Python/src/hexorl/train/trainer.py` | `save_checkpoint`, `load_checkpoint`, `_uncompiled_state_dict` | Trainer writes ad hoc checkpoint dicts and reloads optimizer/scheduler/EMA directly. | Checkpoint schema is not an inspectable contract and training/debug provenance is spread through trainer internals. | Phase 03 | `CheckpointManager` writes strict manifest and bundles train state through registry/family facets. | Save/load/inspect tests, training debug bundle, checkpoint manifest proof, `V2-034`, `V2-035`. |
| Checkpoint cleanup and load compatibility | `Python/src/hexorl/dashboard/checkpoints.py`, `Python/src/hexorl/dashboard/app.py`, `Python/src/hexorl/eval/arena.py` | `_read_checkpoint_metadata`, `_checkpoint_metadata`, `load_checkpoint_model` | Dashboard/eval load checkpoint files directly and infer heads/config from state dicts or legacy `cfg_json`. | User-facing tools can accept stale or incompatible checkpoints that runtime should reject. | Phase 03 for manager; Phase 08 for dashboard/eval consumption | Dashboard/eval inspect through `CheckpointManager` manifests and registry facets. | Dashboard checkpoint display assertions, eval provider coverage, malformed checkpoint rejection tests, `V2-034`, `V2-080`, `V2-082`. |
| Inference architecture dispatch | `Python/src/hexorl/inference/server.py` | `architecture.startswith("global_")`, `_global_graph_mode`, `_is_graph_request`, `_ready_workers_graph_mode` | Server chooses graph mode and required heads from model architecture and shared-memory mode flags. | Request behavior depends on strings/modes instead of protocol manifest; mismatches can route to wrong adapter. | Phase 04 | Dispatch by `request_kind` and negotiated `InferenceProtocolManifest`; no architecture-prefix dispatch. | Handshake/dispatch tests, fake request-kind extension proof, architecture-string audit, `V2-040`, `V2-041`. |
| Inference submit path duplication | `Python/src/hexorl/inference/client.py` | `submit`, `submit_sparse`, `submit_sparse_pair`, `submit_regret_rank`, `submit_graph` | Client owns mode-specific packing, waits, and decoding per request type. | Typed protocol validation, stale slot generations, and telemetry are not centralized. | Phase 04 | Protocol envelope plus request-kind payload schemas; transport owns lifecycle. | Submit lifecycle deletion audit, stale-ready/stale-slot tests, response telemetry assertions, `V2-043`, `V2-045`. |
| Inference transport stale path | `Python/src/hexorl/inference/shm_queue.py` | `req_mode`, `req_ready`, `res_ready`, graph/pair fixed shared arrays | Transport uses ready flags and mode-specific arrays without manifest/sequence-counter ownership. | Stale buffers or mixed request modes can pass as valid responses. | Phase 04 | `shm_transport.py` or documented single owner with sequence counters, finite deadlines, and schema validation. | No indefinite wait tests, stale-ready/stale-slot integration tests, batching/backpressure profile, `V2-043`, `V2-046`. |
| Trainer class branch | `Python/src/hexorl/train/trainer.py` | `isinstance(self.model, GlobalHexGraphNet)`, `_is_global_graph_model`, graph-specific loss defaults and batch path | Trainer switches behavior based on model class and graph-specific output shape. | New families require trainer edits; training target validation is not family adapter-owned. | Phase 03 | Trainer resolves `TrainAdapter` and `LossPlan` from model registry; no model-class/architecture branches. | One-batch every family, branch/import/code audit, pair target validation tests, training debug bundle, `V2-032`, `V2-033`, `V2-035`. |
| Trainer output-key branch | `Python/src/hexorl/train/losses.py` | `compute_losses` switch over head names including `policy_pair_first`, `policy_pair_second`, `policy_pair_joint`, `opp_policy`, sparse/dense graph policy | Loss routing is keyed by output names and target dicts rather than typed family loss plans. | Loss semantics and turn/phase validation can be incomplete for pair/global heads. | Phase 03 | Registry-owned `LossPlan` and `TrainAdapter` validate targets and loss inputs per family. | Loss-plan tests, opening/known-first/joint-pair negative tests, output validation tests, `V2-031`, `V2-033`. |
| Eval dense/direct model path | `Python/src/hexorl/eval/arena.py`, `Python/src/hexorl/eval/players.py` | `model_move_fn`, `NoisyModelPlayer`, `greedy_model_player`, direct `HexNet`, direct `model(tensor)["policy"]`, dense flat index mapping | Evaluation reads dense policy logits directly from model classes and masks with legal bytes. | Eval cannot cover registered non-dense/global families through the same runtime policy path. | Phase 08, dependent on Phase 05 | Evaluation uses `PolicyProvider` for every registered family and routes pair behavior through `PairStrategy`. | Arena/provider tests, no dense-only audit, registered family coverage report, `V2-080`. |
| Dashboard private reconstruction | `Python/src/hexorl/dashboard/replay.py` | `get_replay_position`, `encode_tensor_for_history`, `position_payload`, `_candidate_rows_debug`, `policy_debug` | Dashboard reconstructs board, tensor encoding, legal masks, candidate rows, and policy debug payloads from history. | Displayed model/replay data can diverge from contracts, replay projector, and trainer inputs. | Phase 08 | `ContractInspector` dispatches to focused read-only inspectors over replay/projector/model adapters. | Required route tests, dashboard/training input parity, mismatch owner tests, banned reconstruction audit, `V2-081`, `V2-085`. |
| Dashboard private reconstruction | `Python/src/hexorl/dashboard/model_cache.py` | `infer_history`, direct `load_checkpoint_model`, `encode_tensor_for_history`, `build_candidate_batch`, `build_pair_candidate_batch` | Dashboard loads models and rebuilds candidate/pair input tensors for inference/debug. | Dashboard can create model inputs that training/inference adapters would reject. | Phase 08, with Phase 03/04 dependencies | Dashboard model input/output views use checkpoint manifest, registry inference adapter, and inspector services. | Dashboard model-input parity, checkpoint/protocol display assertions, fake inspector extension proof, `V2-081`, `V2-082`, `V2-085`. |
| Dashboard private reconstruction | `Python/src/hexorl/dashboard/app.py` | `_graph_debug_payload`, `_d6_contract_payload`, `_parse_policy_target_v2`, `_parse_pair_policy_target_v2`, direct `build_graph_batch_from_history`/candidate/pair builders | API routes build graph/candidate/pair/D6 debug payloads in route helpers. | Dashboard route layer becomes a semantic owner and can hide subsystem ownership on mismatch. | Phase 08 | Routes delegate to `ContractInspector` and read-only services only. | Route/import audits, display hash/source/version/trace tests, debug bundle route tests, `V2-081`, `V2-082`, `V2-085`. |
| Autotune raw config mutation | `scripts/run_phase3_48h_autotune.py` | `_cfg_for_trial`, `_apply_dynamic`, direct assignments to `cfg.model.*`, `cfg.buffer.*`, `cfg.selfplay.*`, `cfg.inference.*`, `setattr(trial.dynamic, ...)`, architecture/family conditionals | Autotune mutates raw config objects and branches on family/architecture internals while generating recipes and runtime sweeps. | Trial behavior can bypass typed recipe validation and conflate model semantics with host utilization knobs. | Phase 08 | Typed `ModelRecipe`, `family_spaces.py`, `RuntimeSpec`, scheduler/scoring/manifests/reporting; old script deleted or quarantined outside runtime imports. | Recipe dry-run tests, raw-config mutation audit, runtime sweep watchdog tests, scheduler decision artifacts, `V2-083`, `V2-084`, `V2-086`. |
| Autotune raw config mutation | `Python/src/hexorl/tuning/asha.py`, `Python/src/hexorl/tuning/bohb.py`, `Python/src/hexorl/tuning/pb2.py` | Generic sampler/scheduler dict configs, `asdict` persistence, search-space conditionals | Tuning package is generic but lacks typed recipe/family-space/runtime-spec authority. | Current script-local semantics can keep living outside a typed tuning boundary. | Phase 08 | Make tuning package own typed recipe spaces, runtime sweeps, scoring, manifests, and reporting. | Family-space coverage tests, scheduler score-component artifacts, import audit for old scripts, `V2-083`, `V2-086`. |
| Old replay/buffer runtime | `Python/src/hexorl/buffer/` | `RingBuffer`, `ReplayDataset`, `BufferProcess`, `process_game_record`, old compact arrays/blobs | Runtime replay storage, sampling, target assignment, and buffer process live under old `buffer`. | Old replay decode path remains active for self-play, sampling, training, and epoch runtime. | Phase 07 | Runtime replay path becomes `replay/codec.py`, `storage.py`, `sampler.py`, `projector.py`; old decode only in `tools/migration/` or fixtures. | Replay round-trip/corruption/projector tests, sample-to-loss smoke, buffer import audit, throughput/memory profile, `V2-070` through `V2-075`. |
| Old replay/buffer runtime | `Python/src/hexorl/epoch/pipeline.py` | imports `RingBuffer`, `ReplayDataset`, `process_game_record`; `run_epoch` appends self-play/bootstrap positions to old buffer | Epoch runner wires old buffer directly into training. | End-to-end training can remain on legacy sampler even after new replay exists. | Phase 07 | `run_epoch` uses new replay storage/sampler/projector and train adapters only. | E2E self-play -> replay -> projector -> train smoke, old buffer import audit, `V2-071`, `V2-072`. |
| Old replay/buffer runtime | `Python/src/hexorl/selfplay/orchestrator.py`, `Python/src/hexorl/selfplay/worker.py` | `RingBuffer` ownership, `process_game_record`, record queue writes old `GameRecord` objects | Self-play writes old records into old ring buffer and worker processes targets. | Replay semantics are assembled before contract/replay validation and remain tied to worker internals. | Phase 06 for worker record writer; Phase 07 for replay writer/cutover | `SelfPlayRecordWriter` writes new replay records only; old record processing removed from runtime. | Record writer tests, trace-to-record identity tests, self-play behavior bundle, old buffer import audit, `V2-060`, `V2-070`. |
| Old replay/buffer runtime | `scripts/run_ablation_suite.py`, `scripts/run_phase3_48h_autotune.py`, `scripts/run_restnet_sparse_epoch10.py` | imports `RingBuffer`, `replay_feature_flags`; persistent in-process old replay buffers | Long-running scripts preserve old replay/buffer runtime paths. | Even if package runtime is cut over, operational scripts can keep training/eval on stale semantics. | Phase 07 for replay runtime; Phase 08 for autotune script quarantine; Phase 09 for final CI enforcement | Replace with typed replay/tuning entrypoints or quarantine as offline legacy references outside runtime imports. | Script import audit, tuning dry-run reports, final banned-path CI, `V2-072`, `V2-083`, `V2-091`. |
| Architecture-string gates and deprecated aliases | `Python/src/hexorl/config/schema.py` | `architecture`, `global_architectures`, deprecated `graph` alias normalization, `pair_prior_mix`, `pair_strategy` tied to model config | Config uses architecture names to validate behavior and pair settings. | Model family, pair strategy, and runtime behavior remain config-string coupled. | Phase 03 for model specs; Phase 05 for pair strategy; Phase 08 for typed recipes | Discriminated `ModelSpec`/`ModelRecipe`; `PairStrategySpec` owns pair selection/caps; aliases only in registry/offline migration. | Registry/spec tests, no architecture string audit, default no-pair evidence, recipe dry-run tests, `V2-030`, `V2-053`, `V2-083`. |
| Architecture-string model factory | `Python/src/hexorl/model/network.py`, `Python/src/hexorl/model/global_graph.py` | `build_model_from_config`, `GlobalHexGraphNet.ARCHITECTURES`, `architecture_family`, `if architecture == ...` | Model construction and global variants are multiplexed by architecture strings. | Adding families requires edits in model internals and runtime switch code. | Phase 03 | `models/` registry/facet system; no runtime import from old `hexorl/model`. | Registry matrix tests, fake-family extension proof, no `hexorl/model` runtime import audit, `V2-030`, `V2-031`. |
| Architecture-string self-play/pair gate | `Python/src/hexorl/selfplay/worker.py` | `global_graph_enabled = architecture.startswith("global_")`, `pair_policy_enabled`, `pair_prior_mix` | Worker changes legal radius, inference path, sparse/candidate path, and pair scoring from model config/string state. | Pair scoring or graph path can be accidentally enabled by architecture/config instead of explicit services. | Phase 05 for pair/search; Phase 06 for worker cleanup | `GameRunner` composes `PolicyProvider`, `PairStrategy`, and `EngineAdapter`; no worker architecture logic. | No-implicit-pair tests, no worker architecture logic audit, telemetry pair summary, `V2-053`, `V2-060`, `V2-062`. |
| Direct Rust MCTS and panic/string fallback risk | `Python/src/hexorl/selfplay/worker.py` | `MockMCTSEngine`, `RealMCTSEngine`, direct `_engine.MCTSEngine` calls, `expand_root`, `select_leaves`, `apply_root_pair_*`, `root_generation` handling | Worker wraps and calls Rust MCTS directly and owns mock fallback. | Search errors, stale tokens, pair priors, and root/batch lifecycle can bypass `EngineAdapter` validation/telemetry. | Phase 05 and Phase 06 | `search/engine_adapter.py` is the only Python Rust MCTS caller; worker becomes lifecycle/IPC only. | EngineAdapter import/code audit, stale root/batch token tests, MCTS error structured telemetry, worker MCTS audit, `V2-051`, `V2-056`, `V2-057`, `V2-061`. |

## S4 Baseline Runtime Consumers Mapped

| Runtime slice | Current baseline path | Config-bearing inputs | Primary deletion owners |
|---|---|---|---|
| Self-play write path | `selfplay/orchestrator.py` -> `SelfPlayWorker` -> `process_game_record` -> `RingBuffer` | `cfg.selfplay.*`, `cfg.model.architecture`, `cfg.model.sparse_policy`, `cfg.model.pair_prior_mix`, `cfg.model.pair_strategy`, `cfg.buffer.*`, `cfg.inference.*` | Phase 06 for `GameRunner`/worker cleanup; Phase 07 for record writer/new replay. |
| Replay sampler path | `buffer/ring.py` + `buffer/sampler.py::ReplayDataset` | `BufferConfig`, `TrainConfig`, `ModelConfig.candidate_budget`, `heads`, `sparse_policy`, symmetry flag | Phase 07, with Phase 01/02 contract prerequisites. |
| Training path | `epoch/pipeline.py` -> `ReplayDataset` -> `train/trainer.py` -> `train/losses.py` | `cfg.train.*`, `cfg.model.heads`, `architecture`, loss weights, checkpoint paths | Phase 03 for `TrainAdapter`/loss plan/checkpoint; Phase 07 for replay projector handoff. |
| Evaluation path | `eval/arena.py`, `eval/players.py`, `load_checkpoint_model` | checkpoint `cfg`/`cfg_json`, `Config.model`, direct model policy output | Phase 08, dependent on Phase 03/05. |
| Dashboard path | `dashboard/app.py`, `dashboard/replay.py`, `dashboard/model_cache.py`, `dashboard/checkpoints.py` | run root, dashboard SQLite rows, checkpoint metadata, history bytes, model config | Phase 08, dependent on contracts/replay/model/inference owners. |
| Autotune path | `scripts/run_phase3_48h_autotune.py` plus `tuning/asha.py`, `bohb.py`, `pb2.py` | raw `Config`, BOHB dict configs, family architecture strings, runtime sweep knobs | Phase 08; final CI enforcement in Phase 09. |

## Blocking Evidence Summary

The entries above cannot be claimed deleted until the owning phase supplies all evidence named in the row. In particular:

- Runtime import/code audits must prove deleted or quarantined paths are absent from production imports.
- Replacement modules must be consumed by runtime, not merely present.
- Old-runtime comparison is not sufficient proof; owners need parity, negative/corruption, mutation-safety, and debug/telemetry artifacts named by their phase docs.
- No skipped, deferred, quarantined, or manual-only check closes a deletion row without deterministic replacement coverage and a blocking record.
