# Phase 00 S1 Architecture String And Contract Inventory

Status: evidence only for V2-004 and V2-005 orchestration reconciliation. This file does not mark either row complete and does not implement later-phase architecture.

Scope: S1 Contracts/Schema inventory covering implicit data shapes, config/hash/recipe inputs, architecture-string dependencies, stale runtime paths, and verification risks visible in the current workspace.

Source state note: the search was performed against the current working tree. At the time this artifact was written, pre-existing modifications were present in `Python/src/hexorl/config/schema.py`, `Python/src/hexorl/selfplay/worker.py`, and `Python/tests/test_config_and_guardrails.py`; this S1 artifact did not edit those files.

Blocked search/tool note: `rg --version` failed with exit 1 because `rg.exe` returned `Access is denied`; searches used `git grep` and PowerShell instead. One early misquoted PowerShell `git grep` command failed with exit 1 and was replaced by narrower `git grep` commands.

## Audit Command Evidence

| Command | Exit | Purpose |
| --- | ---: | --- |
| `git status --short` | 0 | Checked baseline dirty state before and during the inventory. |
| `rg --version` | 1 | Confirmed `rg` unavailable due to access denial. |
| `git ls-files Python/src/hexorl` | 0 | Listed Python package surface for S1 search coverage. |
| `git grep -n architecture -- Python/src/hexorl scripts Configs configs` | 0 | Found architecture-string dependencies. |
| `git grep -n global_xattn -- Python/src/hexorl scripts Configs configs Python/tests` | 0 | Found `global_xattn_0` dependencies. |
| `git grep -n pair_prior_mix -- Python/src/hexorl scripts Configs configs Python/tests` | 0 | Found pair-prior config dependencies. |
| `git grep -n pair_strategy -- Python/src/hexorl scripts Configs configs Python/tests` | 0 | Found current pair-strategy guard sites. |
| `git grep -n head_bundle -- Python/src/hexorl scripts Configs configs Python/tests` | 0 | Found recipe/head bundle coupling. |
| `git grep -n -E "compact\|history_bytes\|legal_bytes\|D6\|d6\|symmetry\|mask\|hash\|schema\|version\|finite\|mutation\|MCTS\|root_generation\|batch_generation"` | 0 | Found schema/verification-risk surfaces. |
| `git grep -n -E "recipe\|config_hash\|Config\|model_config\|runtime\|checkpoint\|sha256\|hashlib\|yaml\|toml\|json"` | 0 | Found config, recipe, checkpoint, and hash inputs. |
| `git grep -n -E "_py_decode_compact_record\|_py_apply_d6_symmetry\|_transform_history_bytes\|encode_compact_record\|apply_d6_symmetry\|legal_bytes\|history_bytes"` | 0 | Found duplicate history/D6/protocol helpers. |

## Required Inventory

| File path | Symbol/pattern | Current behavior | Risk for V2-004/V2-005 | Owner phase | Expected deletion/replacement | Blocking tests/evidence |
| --- | --- | --- | --- | --- | --- | --- |
| `Python/src/hexorl/config/schema.py:23,43-66` | `ModelConfig.architecture`, `global_architectures`, `arch == "graph"` alias | Validates a string enum for `cnn`, `restnet`, `graph_hybrid_0`, and hard-coded global graph names; normalizes deprecated `graph` to `graph_hybrid_0`. | Config schema currently acts as a model-family registry and compatibility mapper. Old aliases can survive as runtime input unless deletion is tested. | Phase 03, Phase 09 | Replace with typed model family/recipe registry validation; remove `graph` runtime alias or quarantine it as non-runtime migration tooling. | Registry matrix tests; config rejection test for legacy alias after migration; import/code search proving no runtime branch depends on raw architecture strings. |
| `Python/src/hexorl/config/schema.py:73,139-142` | Attention gates by `architecture` | Requires channel divisibility for restnet/graph/global strings; allows `attention_positions` only for `restnet`. | Model capability rules are inferred from string names instead of a registry facet. | Phase 03 | Move capability validation into model-family descriptors and `ModelRecipe`. | Fake-family extension test proving attention capability is registered without editing `Config`; no string branch audit. |
| `Python/src/hexorl/config/schema.py:37-39,106-129,248-262` | `pair_prior_mix`, `pair_strategy`, `pair_strategy_max_pairs`, pair heads | `pair_strategy` defaults to `none`; non-none strategy requires positive mix/max rows and an explicit pair-policy head. | Phase 00 guard exists in schema, but pair semantics are still model config fields rather than a `PairStrategySpec`. Verification must prove `pair_prior_mix` or heads alone cannot enable scoring. | Phase 05, with Phase 00 evidence | Replace with `PairStrategySpec` validation and explicit `PairStrategy` runtime owner. | No-implicit-pair tests; diagnostic full-pair cap tests; logs showing `pair_rows_possible` and `pair_rows_scored`; search audit for pair enablement by architecture/head/mix. |
| `Python/src/hexorl/config/schema.py:289-320` | Global architecture auto-loss defaults | If `model.architecture` is in a hard-coded global set, loss weights and lookahead heads are filled automatically. | Training target/loss plan is inferred from architecture string instead of model train adapter capabilities. | Phase 03 | Move loss defaults and head activation to registry `TrainAdapter`/loss-plan facet. | One-batch-per-family tests; fake-family extension proof; no `global_architectures` branch in trainer/config runtime. |
| `Configs/*.toml` | Omitted `model.architecture`; raw TOML heads/loss/runtime knobs | Current checked-in configs generally rely on schema default `architecture = "cnn"` and raw heads/loss weights. | Baseline command hashes must distinguish raw config, resolved schema defaults, and runtime autotune changes; absence of explicit architecture can hide behavior changes. | Phase 00 for baseline evidence; Phase 03/08 for typed recipes | Record both raw file hash and resolved `Config.model_dump()` hash; later replace raw config mutation with typed recipes. | `CONFIG_HASH_INDEX.md` entries; recipe dry-run artifacts; tests proving resolved recipe identity changes when defaults change. |
| `Python/src/hexorl/model/network.py:544-610` | `HexNet.__init__(architecture=...)`, `graph` alias, `restnet` attention branch | One model class switches trunk behavior based on string values and keeps `graph` compatibility normalization. | Runtime model construction can keep old and new paths alive together; graph/restnet behavior is private to class internals. | Phase 03 | Split into registered model-family builders/facets; delete runtime alias handling from model class. | Registry build tests for every family; no direct architecture string branch audit; checkpoint strict-load tests by manifest family. |
| `Python/src/hexorl/model/network.py:856-891` | `from_config`, `GlobalHexGraphNet.ARCHITECTURES` dispatch | Factory dispatches to `GlobalHexGraphNet` if architecture string is in its set; otherwise builds `HexNet`. | Model factory is a central string switch, blocking registry ownership and protocol manifests. | Phase 03, Phase 04 | Replace with registry lookup and inference/train adapter descriptors. | Import/code audit proving runtime does not import `hexorl/model` directly after registry cutover; fake-family extension proof. |
| `Python/src/hexorl/model/global_graph.py:132-185,239-285` | `ARCHITECTURES`, `architecture_family`, per-architecture forward branches | Global graph variants are encoded as strings inside one class; relation requirements, block counts, and forward logic branch by architecture. | Model-family semantics, graph protocol requirements, and inference behavior are entangled. | Phase 03, Phase 04 | Move variants to family descriptors/facets and protocol payload schemas; keep model forward driven by registered capability objects. | Registry tests for each graph family; protocol manifest tests; no architecture branch in inference dispatch. |
| `Python/src/hexorl/inference/server.py:63-65` | `architecture.startswith("global_")` | Inference server decides graph mode and required heads from architecture prefix. | Inference dispatch uses string inference instead of a manifest/request kind; protocol mismatch can be hidden as missing heads or wrong path. | Phase 04 | Replace with `InferenceProtocolManifest` and request-kind payload schemas. | Handshake and manifest validation tests; architecture-string audit; protocol mismatch fail-fast tests. |
| `Python/src/hexorl/selfplay/worker.py:1174-1185,1390-1426` | `global_graph_enabled`, `pair_strategy`, `pair_policy_enabled` | Worker enables global graph mode from `architecture.startswith("global_")`; current pair scoring is controlled by `pair_strategy != none`. | Self-play still owns model-family dispatch and pair scoring decisions that should belong to `PolicyProvider`, `PairStrategy`, and `EngineAdapter`. | Phase 05, Phase 06 | Move policy mapping to `PolicyProvider`, pair selection/caps/scoring to `PairStrategy`, and game loop composition to `GameRunner`. | No pair scoring without strategy; policy-to-legal-row debug bundle; worker import audit showing no builder internals or architecture branches. |
| `Python/src/hexorl/selfplay/worker.py:126-185,238-300` | `_score_graph_pair_chunks`, `_score_crop_pair_chunks` | Pair rows are scored through chunk helpers with explicit `max_pair_rows`; errors if cap is nonzero/invalid. | Pair scoring mechanics are private worker helpers, so later phases need deletion proof and replacement through `PairStrategy`. | Phase 05 | Replace with registered `PairStrategy` and cap validation (`root`, `leaf`, `full`, diagnostic). | Pair cap rejection tests; full-pair diagnostic tests; code search proving chunk helpers are deleted or no longer runtime-reachable. |
| `Python/src/hexorl/buffer/ring.py:22-96` | `GLOBAL_GRAPH_ARCHITECTURES`, `replay_feature_flags` | Replay storage flags include sparse diagnostics if heads, sparse policy, graph flag, or architecture string indicate graph/global. | Replay record shape is inferred from model strings/heads instead of a replay contract version. | Phase 07 | Replace with new replay record schema and projector-owned feature requirements. | New replay writer/sampler tests; old buffer import audit; schema/hash/source assertions on replay records. |
| `Python/src/hexorl/buffer/process.py:75-90` | `graph=str(...architecture...).startswith("global_")` | Buffer process passes graph storage flag based on architecture prefix. | Background replay storage path can silently change data shape from config string. | Phase 07 | Move storage feature selection to replay schema/record writer contract. | Runtime smoke writing new replay records; import audit proving old buffer runtime ownership removed for sampler/train paths. |
| `Python/src/hexorl/epoch/pipeline.py:175-185,317-323` | `include_graph_policy=str(...architecture...).startswith("global_")` | Training dataset includes graph policy tensors based on architecture prefix. | Train data projection is architecture-driven and can diverge from model adapter requirements. | Phase 03, Phase 07 | `TrainAdapter` declares required batch fields; replay projector emits typed projections. | One-batch every family; sample-to-loss tests; no dataset branch by architecture audit. |
| `Python/src/hexorl/train/trainer.py:19-96,238-251` | `isinstance(GlobalHexGraphNet)`, graph default losses, graph tensor checks | Trainer branches on model class to apply graph defaults and require graph tensors. | Trainer consumes model internals directly instead of `TrainAdapter`; string replacement alone is insufficient. | Phase 03 | Replace with `TrainAdapter` and model-family loss plan. | One-batch every registered family; missing graph tensor negative tests through adapter; no `GlobalHexGraphNet` trainer import. |
| `Python/src/hexorl/train/trainer.py:437-451` | `cfg_json`, `model_metadata`, `action_contract_metadata` checkpoint payloads | Checkpoints persist JSON config and model metadata, but stable recipe/contract identity is not yet a public contract. | Checkpoint load/inspect can rely on mutable raw config snapshots rather than strict manifest identity. | Phase 03 | `CheckpointManager` manifest with strict load, model family, recipe hash, protocol/contract versions. | Manifest round-trip; inspect without weights; duplicate prefix cleanup audit; config/recipe hash examples. |
| `Python/src/hexorl/eval/arena.py:203-211` | `load_checkpoint_model`, `from_config` | Eval reconstructs model via checkpoint config or current config and calls `from_config`. | Eval remains tied to model factory and architecture string, not `PolicyProvider`. | Phase 08 | Evaluation uses `PolicyProvider` for every registered family. | Arena/provider tests; no dense-only/model-factory audit. |
| `Python/src/hexorl/dashboard/app.py:789-905,1097-1120` | `architecture`, `architecture_summary` | Dashboard reads family/model metadata and summarizes architecture via string branches for graph/restnet/CNN. | Dashboard reconstructs semantics from private model metadata instead of read-only contract inspectors. | Phase 08 | `ContractInspector` dispatcher and read-only model/checkpoint/protocol inspector services. | Route tests; screenshot/artifact where relevant; inspector extension proof; no private sampler/model reconstruction audit. |
| `Python/src/hexorl/dashboard/checkpoints.py:78-119,130-139` | Checkpoint SHA and `_heads_from_state_dict` | Indexes checkpoint file SHA and infers heads from config, metadata, or state-dict prefixes. | Checkpoint identity is file-level, while model family/recipe/protocol identity can be inferred heuristically. | Phase 03, Phase 08 | `CheckpointManager` inspect manifest plus dashboard inspector. | Strict manifest tests; state-dict prefix cleanup/deletion audit; dashboard displays manifest identity. |
| `Python/src/hexorl/dashboard/model_cache.py:23-180` | Imports `HexNet`, sparse/pair head debug branches | Dashboard model cache executes model/debug logic directly and branches by available heads. | Dashboard can become a private inference/model-input reconstruction path. | Phase 08 | Read-only inspector services and provider-backed eval/play path. | Dashboard route tests; banned private model-input reconstruction audit. |
| `Python/src/hexorl/runtime.py:225-230` | Runtime knob selection by `architecture` | Runtime autotune adjusts compile/runtime defaults for `restnet`, `graph`, and `graph_hybrid_0`. | Host utilization policy is coupled to model string semantics. | Phase 08 | Typed `RuntimeSpec` separates host knobs from model recipe semantics. | Runtime sweep dry-run tests; scheduler score artifacts; no raw config mutation audit. |
| `Python/src/hexorl/inference/client.py:301-310,327-348` | Graph IPC `req_graph_meta`, count/capacity writes | Client packs schema versions and counts into shared memory, then copies NumPy projections into fixed arrays. | Protocol is implicit in slot layout and counts; no request-kind manifest/hash/sequence identity. | Phase 04 | `InferenceProtocolManifest` plus transport-owned sequence counters and payload schema validation. | Stale buffer/slot tests; protocol version/kind mismatch tests; telemetry assertions including schema/source/hash. |
| `Python/src/hexorl/inference/server.py:500-530,585-603,760-767` | Shared-memory decode to Torch tensors | Server decodes graph/candidate arrays by shape/count and wraps host NumPy buffers with `torch.from_numpy`. | Mutation/stale-buffer risks after validation; shape-only checks are insufficient. | Phase 04 | Transport owner validates slot generations, sequence counters, schema ids, counts, finiteness, and immutable/copy policy. | Inference debug bundle; stale-ready/stale-slot integration tests; post-validation mutation tests. |
| `Python/src/hexorl/graph/batch.py:21-28,76-106,488-828,1202-1278` | `GraphBatch`, schema constants, builder/collator | Graph semantic construction, tensor fields, pair rows, relation tensors, and collation live in one module with schema version integers. | Semantic graph ownership and tensorization/collation are not separated; version/hash/source identity is not enough for V2 closure. | Phase 02 | Split semantic builder from tensorize/collate projections; add contract source/hash/row identity. | Projection tests; graph schema tests; banned private rebuild audit; D6 and corruption tests. |
| `Python/src/hexorl/action_contract/candidates.py:38-72,90-407` | `CandidateBatch`, `PairCandidateBatch`, builders | Candidate/pair data shapes are frozen dataclasses with NumPy arrays but no schema/source/hash identity. | Current contracts can be mutated through arrays and are not yet canonical V2 contracts. | Phase 02 | Candidate and pair contracts with row identity, schema version, hash, immutable/mutation-guarded arrays, registered extensions. | Golden equality tests; extension-proof tests; mutation/corruption tests; self-play/replay/train/dashboard consumption proof. |
| `Python/src/hexorl/buffer/sampler.py:249-365,637-952` | `_py_decode_compact_record`, `_py_apply_d6_symmetry`, `_transform_history_bytes`, `_encode_tensor_meta` fallback | Replay sampler duplicates compact-history decoding, D6 transforms, and Python fallback legal/tensor generation; caches tensors and legal bytes. | Duplicate legal/history/D6 implementations are legacy fallback paths and mutation-risk caches. | Phase 01, Phase 07 | Engine/history/D6 contracts in Phase 01; replay projector and new record schema in Phase 07. | Fallback quarantine/deletion proof; malformed history tests; D6 parity; replay projection mutation tests. |
| `Python/src/hexorl/action_contract/tactical_oracle.py:46-84` | `parse_history_state`, `engine_game_from_history` | Tactical oracle parses compact `(player,q,r)` bytes and replays through engine when available. | Duplicate parser is useful as a diagnostic but cannot be runtime semantic authority after contracts. | Phase 01 | Route production history validation through engine/history contract; keep only approved test/debug helper if needed. | Engine-required oracle tests; malformed-byte tests; import audit proving no runtime fallback parser. |
| `crates/hexgame-py/src/protocol.rs:19-69` | `decode_legal_rows`, `decode_compact_history_rows`, `decode_pair_rows` | Rust FFI crate owns legal/history/pair byte decoding helpers. | Rust is canonical production boundary but not self-validating; Python contracts still need schema/source/hash/semantic validation. | Phase 01, Phase 04, Phase 09 | Python engine/contracts wrap FFI outputs with version/source/hash and negative validation; final public API drift checks. | Malformed legal/history/pair byte tests; Rust/Python semantic identity tests; protocol source/hash assertions. |
| `scripts/run_phase3_48h_autotune.py:58-75,200-245,833-941,1605-1685,2415-2465` | `GLOBAL_GRAPH_ARCHITECTURES`, `FamilySpec.architecture`, `StaticRecipe`, `_make_config`, head bundles | Autotune defines families/recipes with architecture strings, mutates raw `Config`, assigns heads/loss/runtime knobs, and uses JSON keys for candidate de-dup/cache. | Autotune can mutate model semantics through raw config fields and recipe inputs are not yet typed, hashed public contracts. | Phase 08, Phase 09 | Typed `ModelRecipe`, family spaces, `RuntimeSpec`, stable recipe/config hash artifacts. | Recipe dry-run tests; raw-config mutation audit; scheduler score-component artifacts; hash examples. |
| `scripts/run_restnet_sparse_epoch10.py:41-68` | Hard-coded `cfg.model.architecture = "restnet"` and raw config mutation | Script forces a model family and sparse settings by direct config mutation. | Side-channel experiment scripts can preserve architecture-string behavior after runtime refactor. | Phase 08, Phase 09 | Replace with typed recipe invocation or quarantine as non-runtime historical tooling. | Script inventory/deletion/quarantine manifest; recipe dry-run replacement artifact. |

## Reconciliation Notes

- V2-004 evidence: the table identifies architecture strings, duplicate helpers, implicit contract shapes, stale compatibility aliases, and owner phases for replacement/deletion.
- V2-005 evidence: the risks and blocking evidence columns name where old-runtime comparison is insufficient and what independent, negative, mutation, schema, hash, and debug-bundle evidence must exist before later closure.
- No runtime code, tests, matrix rows, or other Phase 00 artifacts were changed by this S1 update.
