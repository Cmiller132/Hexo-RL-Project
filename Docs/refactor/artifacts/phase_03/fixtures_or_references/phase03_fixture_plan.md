# Phase 03 Fixture And Artifact Plan

Required fixtures and artifacts:

- registered family list with capabilities
- default recipe validation output for every family
- one-batch trainer smoke output for every family or documented representative set
- fake-family descriptor extension example
- checkpoint manifest round-trip sample
- checkpoint inspect-without-weights proof
- pair target validation proof for opening, first-placement, second-placement known-first, D6, stale legal rows, and corrupt masks
- single-position training debug bundle showing replay record, contracts, tensors, targets, outputs, losses, hashes, and trace ids
- training adapter projection/device-transfer performance JSON
